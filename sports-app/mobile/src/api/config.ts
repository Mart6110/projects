// Expo Go on a phone can't reach "localhost" — that resolves to the phone
// itself, not your dev machine. Set this to your computer's LAN IP (e.g.
// http://192.168.1.20:8080) so a physical device or simulator can reach the
// Go backend. See mobile/README.md for how to find your IP.
export const API_BASE_URL = "http://192.168.1.100:8080/api";
